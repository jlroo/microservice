spring:
  application:
    name: eureka

server:
  port: 8761

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://localhost:${server.port}/eureka/

management:
  security:
    enabled: false
